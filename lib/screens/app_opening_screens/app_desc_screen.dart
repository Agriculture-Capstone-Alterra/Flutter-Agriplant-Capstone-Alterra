import 'package:flutter/material.dart';

class AppDesc extends StatefulWidget {
  const AppDesc({Key? key}) : super(key: key);

  @override
  State<AppDesc> createState() => _AppDescState();
}

class _AppDescState extends State<AppDesc> {
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [

      ],
    );
  }
}
